# Migration validation report (synthetic demo)

This report is regenerated by `examples/migration_case/run_demo.sh`. 
It summarizes local, offline analysis for the fixture SQL 
`legacy_postgres_query.sql`: parse readiness, CTE footprint, 
dependency wiring, and a lightweight structural migration-risk label.

## Fixture

- Source dialect (for SQLGlot read): Postgres

## Parse readiness

- Valid parse: `True`
- Statement count (top-level parsed): `1`
- Validation errors: `0`

## Structural cues

Notes:

- Query contains five or more CTEs.

## Deterministic migration risk signals

- Canonical bucket (mirrors CLI `risk_level`): **medium**
- Tagged categories (2): `deep_cte_chain`, `postgres_cast_syntax`

Structured findings:

- `deep_cte_chain` · *medium*: Query declares five or more CTE definitions.
- `postgres_cast_syntax` · *medium*: PostgreSQL :: cast syntax may need dialect-specific review.

This artifact does **not** apply target dialect rewrites—it only inventories parse + graph context for reviewers.

## CTE footprint

- CTE definitions: `6`
- Dependencies edges (CTE/table): `7`
- Outer table refs (non-CTE): `3`
- CTE cycle detected in graph: `False`

## Dependency graph excerpt

Directed edges:

- `base_events` → `metric_fact_sample` (cte_to_table)
- `geo_enriched` → `base_events` (cte_to_cte)
- `geo_enriched` → `geography_dim_sample` (cte_to_table)
- `geo_keys` → `base_events` (cte_to_cte)
- `product_enriched` → `geo_enriched` (cte_to_cte)
- `product_enriched` → `product_dim_sample` (cte_to_table)
- `product_keys` → `base_events` (cte_to_cte)

## Rewrite / conversion status

- Registered deterministic rewrite passes: `0` 
(none applied in this MVP analysis path)

## Not covered by this harness

- Semantic validation against a live warehouse catalog
- Cost or SLA guarantees
- Automatic cross-dialect SQL rewriting
- LLMs, MCP tooling, credentials, or network calls
